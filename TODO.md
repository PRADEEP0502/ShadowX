# TODO
- [ ] Fix ChatScreen layout/constraints so PremiumChatBubble shows message text, correct bubble sizing, and timestamp placement.
- [ ] Ensure chat-area text theme forces white readable text on dark bubbles (no widget API changes).
- [ ] Verify no backend/websocket logic was modified.
- [ ] Run `flutter analyze` (and optionally `flutter test`) and visually confirm the chat UI.

